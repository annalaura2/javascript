const notas = [];

notas.push(5); 
notas.push(7); 
notas.push(8);

console.log(notas.length); // mostra o tamanho da lista

// estrutura de repetição for 
for (let i = 0; i < 10 ; i++) {
    console.log(i);
}